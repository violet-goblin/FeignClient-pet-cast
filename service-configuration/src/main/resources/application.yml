server:
  port: 8888

spring:
  application:
    name: configuration
  cloud:
    config:
      # file://다음에 msaconfig.yml 파일 경로를 absolute path로 추가해 놓을 것
      server:
        git:
          uri: file://C:\Users\playdata0\Documents\pet_cast_feign\config